apiVersion: v1
kind: Service
metadata:
  name: polymarket-mcp
  namespace: default
  labels:
    app: polymarket-mcp
spec:
  type: ClusterIP
  selector:
    app: polymarket-mcp
  ports:
  - name: http-mcp
    port: 8000
    targetPort: 8000
    protocol: TCP

  # Session affinity (optional)
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 3600
